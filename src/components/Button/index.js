import React, { Component } from 'react'
import { Button } from 'antd'
export default class index extends Component {
  render() {
    return (
      <div>
        <Button type={'primary'}>导出的按钮</Button>
      </div>
    )
  }
}
